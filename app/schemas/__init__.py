"""API schemas (Pydantic request/response models).

These validate incoming JSON and serialize outgoing JSON. They are the
API layer's contract -- distinct from ORM models (persistence) and from
service arguments (business logic).
"""
